# example_chef_policy_archive

This repository contains a simple Chef Infra Policy that can be consumed by systems along with an associated Policy Archive.

Uses `log_hello` cookbook from the Chef Supermarket
